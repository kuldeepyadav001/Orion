{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capabilities for the Orion window. Deliberately narrow: the webview is untrusted and gets no filesystem, no HTTP and no arbitrary shell access. Every privileged action goes through an explicit Rust command. The global shortcut is registered from Rust only, so the renderer gets no shortcut permissions at all.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging",
    "dialog:allow-open",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-unminimize",
    "core:window:allow-is-visible",
    "core:window:allow-is-focused"
  ]
}
